export default function ExampleModuleDetailPage() {
  return (
    <div>
      <h1 style={{ marginTop: 0 }}>Módulo de ejemplo — Detalle</h1>
      <p style={{ color: "var(--color-text-secondary)" }}>
        Sub-página de ejemplo, referenciada desde <code>subItems</code> en el
        config de navegación.
      </p>
    </div>
  );
}
